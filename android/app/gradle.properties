android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Backend configuration (override in local.properties or with -P flags).
# These become BuildConfig fields. INBOUND_TOKEN must match one of the backend's
# INBOUND_TOKENS (or CRON_SECRET) during the team-testing phase.
BASE_URL=https://simpleexpensereport.vercel.app
INBOUND_TOKEN=
